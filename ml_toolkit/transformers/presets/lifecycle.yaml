# Пресет: жизненный цикл и активность клиента.
# Задачи: retention, отток, сегментация по стадии жизненного цикла.
# Глубина истории: до 24 месяцев.

# ── Стаж и возраст ───────────────────────────────────────────
tenure: {}

client_age: {}

lifecycle_phase:
  windows: [12]

# ── Недавность и паузы ───────────────────────────────────────
recency: {}

inactive_streak: {}

# ── Регулярность активности ──────────────────────────────────
activity_rate: {}

active_months:
  windows: [6, 12, 24]

active_run_count:
  windows: [6, 12]

longest_active_run:
  windows: [6, 12]

streak: {}

# ── Доля нулей ───────────────────────────────────────────────
zero_share:
  windows: [3, 6, 12]

zero_clustering:
  windows: [12]

# ── Восстановление после провала ─────────────────────────────
recovery_dynamics:
  windows: [12]
