# Расширенный пресет для модели чувствительности к скидке / предсказания размера скидки.
# Надстройка над discount_sensitivity.yaml: та же гипотеза "текущее состояние важнее
# всего", плюс качество/устойчивость тренда, память ряда, фазы жизненного цикла,
# резкие сдвиги уровня. 65 колонок на product-колонку (780 на 12 колонок) -
# примерно в 2 раза больше кандидатов Phase A, чем базовый пресет.
#
# Тот же принцип сегментации: НЕ сегментируем то, что само описывает паттерн
# активности/разрывов (recency/tenure/inactive_streak/activity_rate/zero_share/
# active_run_count) - там разрыв и есть сигнал. Сегментируем всё, что описывает
# динамику ЗНАЧЕНИЯ - там долгий нулевой разрыв исказил бы сигнал.
#
# Заполнение NaN — как в discount_sensitivity.yaml: не здесь, а в FILL_NAN
# кернела каждого трансформера, применяется отдельно через fill_known_nan(df)
# после отбора (см. её комментарий в feature_generation.py).

segments:
  medium_gap:
    strategy: zero_gap
    gap_threshold: 8

# -----------------------------------------------------------------------------
# Текущее состояние / активность
# -----------------------------------------------------------------------------

segment_gap:
  segment: medium_gap

recency: {}

tenure: {}

inactive_streak: {}

activity_rate: {}

zero_share:
  windows: [3, 12]

active_run_count:
  windows: [12]                # фрагментация активности: много коротких вспышек vs один long run

# -----------------------------------------------------------------------------
# Уровень / тренд / волатильность значения
# -----------------------------------------------------------------------------

window_mean:
  windows: [3, 6, 12]
  segment: medium_gap           # простой интерпретируемый уровень

window_median:
  windows: [3, 6, 12]
  segment: medium_gap           # устойчивая к выбросам версия уровня

rolling_min_max:
  windows: [3, 6, 12]
  segment: medium_gap           # диапазон разброса значения за период

log_level:
  windows: [3, 12]
  segment: medium_gap

slope:
  windows: [3, 12]
  segment: medium_gap

accel:
  segment: medium_gap

growth_since_start:
  segment: medium_gap

distance_to_global_max:
  segment: medium_gap

cumulative_share:
  segment: medium_gap           # доля текущей точки в накопленной сумме текущего сегмента активности

rolling_cv:
  windows: [3, 12]
  segment: medium_gap

rolling_std:
  windows: [3, 12]
  segment: medium_gap           # абсолютная волатильность рядом с нормированной CV - для
                                # предсказания РАЗМЕРА скидки масштаб может быть важен сам по себе

# -----------------------------------------------------------------------------
# Память / качество тренда / структурные сдвиги - новое относительно базового пресета
# -----------------------------------------------------------------------------

autocorr:
  segment: medium_gap           # инерционность/предсказуемость динамики (лаги 1/2/3/6/12)

trend_consistency:
  windows: [12]
  segment: medium_gap           # тренд - устойчивый сигнал или шум (R², clean_streak)

regime_change:
  windows: [12]
  segment: medium_gap           # резкий сдвиг уровня - возможный маркер пересмотра условий

lifecycle_phase:
  windows: [12]
  segment: medium_gap           # фаза (разгон/зрелость/спад) - разная фаза = разная позиция в переговорах

extreme_events:
  windows: [12]
  segment: medium_gap           # заменяет extreme_share базового пресета: та же гипотеза
                                # "промо-driven всплески", но с разбивкой на всплеск/обвал/давность
