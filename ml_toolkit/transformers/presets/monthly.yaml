# Пресет для месячной гранулярности.
# Имена ключей совпадают с FEATURE в соответствующем модуле.

# ── simple statistics ───────────────────────────────────────
window_mean:
  windows: [3, 6, 12]

window_median:
  windows: [3, 6, 12]

# ── trend ───────────────────────────────────────────────────
slope:
  windows: [6, 12, 24]

slope_ratio:
  pairs:
    - [6, 12]
    - [12, 24]

momentum:
  half_windows: [3, 6]

direction_flag:
  windows: [6, 12]

max_abs_jump:
  windows: [6, 12]

streak: {}

growth_since_start: {}

# ── volatility ──────────────────────────────────────────────
rolling_std:
  windows: [6, 12, 24]

rolling_cv:
  windows: [6, 12, 24]

rolling_min_max:
  windows: [6, 12]

skew_proxy:
  windows: [6, 12]

extreme_share:
  windows: [6, 12]

# ── tenure / activity ────────────────────────────────────────
tenure: {}

recency: {}

inactive_streak: {}

active_months:
  windows: [6, 12, 24]

longest_active_run:
  windows: [6, 12]

client_age: {}

activity_rate: {}

zero_share:
  windows: [3, 6, 12]

active_run_count:
  windows: [6, 12]

# ── dynamics ratios ──────────────────────────────────────────
lag1_diff: {}

rolling_sum:
  windows: [3, 6, 12]

mean_median_gap:
  windows: [6, 12]

recent_share:
  pairs:
    - [3, 12]
    - [6, 24]

zscore:
  windows: [6, 12, 24]

ewma:
  alphas: [0.3]

# ── trend change ─────────────────────────────────────────────
accel: {}

sign_change_count:
  windows: [6, 12]

trend_flip:
  lag_window_pairs:
    - [6, 6]
    - [12, 12]

max_drawdown:
  windows: [6, 12, 24]

peak_trough_timing:
  windows: [6, 12]

volatility_of_diff:
  windows: [6, 12]

run_above_mean:
  window: 12

time_weighted_momentum:
  windows: [6, 12]

# ── relative position ────────────────────────────────────────
pct_of_max:
  windows: [6, 12, 24]

rank_in_window:
  windows: [6, 12, 24]

trough_to_current:
  windows: [6, 12]

local_extrema:
  windows: [6, 12]

cumulative_share: {}

level_ratio:
  pairs:
    - [3, 12]
    - [6, 24]

log1p_level: {}

distance_to_global_max: {}

volatility_trend:
  pairs:
    - [3, 12]
    - [6, 12]

half_ratio:
  windows: [6, 12]

# ── structural signals ───────────────────────────────────────
cusum:
  windows: [6, 12]

corr_with_time:
  windows: [6, 12]

entropy:
  windows: [6, 12]

gini:
  windows: [6, 12]

lag_growth_ratio:
  lags: [3, 6, 12]

# ── autocorrelation & seasonal ───────────────────────────────
autocorr: {}

seasonal_autocorr: {}

# ── log growth ───────────────────────────────────────────────
log_slope:
  windows: [6, 12, 24]

log_slope_ratio:
  pairs:
    - [6, 12]

log_volatility:
  windows: [6, 12]

geometric_return:
  windows: [6, 12]

log_level:
  windows: [6, 12]

# ── smoothness ───────────────────────────────────────────────
total_variation:
  windows: [6, 12]

alternation_rate:
  windows: [6, 12]

roughness_ratio:
  pairs:
    - [6, 12]

# ── distribution moments ─────────────────────────────────────
kurtosis_proxy:
  windows: [6, 12]

# ── extreme events ───────────────────────────────────────────
extreme_events:
  windows: [12]

# ── plateau ──────────────────────────────────────────────────
plateau:
  windows: [6, 12]

# ── regime change ────────────────────────────────────────────
regime_change:
  windows: [12]

# ── lifecycle phase ──────────────────────────────────────────
lifecycle_phase:
  windows: [12]

# ── lag comparison ───────────────────────────────────────────
lag_comparison: {}

# ── burstiness ───────────────────────────────────────────────
burstiness:
  windows: [12]

# ── zero clustering ──────────────────────────────────────────
zero_clustering:
  windows: [12]

# ── recovery dynamics ────────────────────────────────────────
recovery_dynamics:
  windows: [12]

# ── trend consistency ────────────────────────────────────────
trend_consistency:
  windows: [6, 12]

# ── window volatility ratios ─────────────────────────────────
window_volatility_ratios: {}

# ── cross-window momentum ────────────────────────────────────
cross_window_momentum: {}

# ── value clustering ─────────────────────────────────────────
value_clustering:
  windows: [12]

# ── flow regularity ──────────────────────────────────────────
flow_regularity:
  windows: [12]

# ── growth quality ───────────────────────────────────────────
growth_quality:
  windows: [12]

# ── mean deviation shape ─────────────────────────────────────
mean_deviation_shape:
  windows: [6, 12]

# ── nonlinearity ─────────────────────────────────────────────
nonlinearity:
  windows: [6, 12]

# ── quantile persistence ─────────────────────────────────────
quantile_persistence:
  windows: [12]

# ── microstructure ───────────────────────────────────────────
microstructure:
  windows: [12]
